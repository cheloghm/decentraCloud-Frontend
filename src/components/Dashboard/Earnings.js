// src/components/Dashboard/Earnings.js

import React from 'react';

const Earnings = () => {
    return (
        <div style={styles.container}>
            <h2>Earnings</h2>
            {/* Earnings chart will go here */}
        </div>
    );
};

const styles = {
    container: {
        padding: '20px',
        marginLeft: '220px'
    }
};

export default Earnings;
