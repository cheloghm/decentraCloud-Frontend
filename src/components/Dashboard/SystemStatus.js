// src/components/Dashboard/SystemStatus.js

import React from 'react';

const SystemStatus = () => {
    return (
        <div style={styles.container}>
            <h2>System Status</h2>
            {/* System status metrics will go here */}
        </div>
    );
};

const styles = {
    container: {
        padding: '20px',
        marginLeft: '220px'
    }
};

export default SystemStatus;
